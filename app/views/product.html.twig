{% extends 'base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-8">
            <div class="panel panel-primary">
                <div class="panel-heading">{{ product.name }}</div>
                <div class="panel-body"><img src="https://placehold.it/300x160?text={{ product.code }}" class="img-responsive" style="width:100%" alt="Image"></div>
                <div class="panel-footer">{{ product.description }}</div>
            </div>
        </div>
        <div class="col-sm-4">
            <form method="post">
                <input type="hidden" value="{{ product.code }}">
                <div class="container button-wrapper">
                    <button class="btn btn-lg" data-toggle="tooltip" data-placement="bottom">Añadir al carrito <span class="glyphicon glyphicon-shopping-cart"></span></button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
